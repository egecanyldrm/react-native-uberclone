import React from 'react'
import { Text, View } from 'react-native'

type Props = {}

const CommonButton = (props: Props): JSX.Element => {
    return (
        <View>
            <Text> button</Text>
        </View>
    )
}

export default CommonButton